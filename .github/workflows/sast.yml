name: Security
on:
  push:
    branches:
      - 'master'
jobs:
  mixeway-scan:
    name: Mixeway Scanning
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Prepare variables
        id: vars
        shell: bash
        run: |
          echo "##[set-output name=branch;]$(echo ${GITHUB_REF#refs/heads/})"
          echo "::set-output name=sha_short::$(git rev-parse HEAD)" 
      - name: Prepare Mixeway docker image
        run: |
          docker pull mixeway/scanner:latest
      - name: Run Scan
        run: |
          docker run -e OSS_USERNAME=wanda1111111@gmail.com -e OSS_KEY=619d704d395bfa3d01a2a05a39365885ad356e7b -e MIXEWAY_URL=https://hub.mixeway.io -e MIXEWAY_KEY=d9664902-e221-4572-bebd-dcf6734c5417 -e MIXEWAY_PROJECT_ID=95 -e MODE=STANDALONE -e MIXEWAY_PROJECT_NAME=S01133257_lab_secure_app -e COMMIT_ID=test -e BRANCH=master -v /opt/lab_sec_audit/sast:/opt/sources mixeway/scanner:latest 
